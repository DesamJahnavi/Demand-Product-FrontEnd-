import React from 'react';
import { Gauge, Sparkles, TrendingUp, Users } from 'lucide-react';
import { trendData } from '../../data/mockData';
import DemandChart from '../charts/DemandChart';
import StatCard from '../common/StatCard';
import ViewHeader from '../common/ViewHeader';
import UserProfile from './UserProfile';
export default function UserDashboard({ products, profile, onLogout }) { return <main className="user-dashboard"><header className="user-topbar"><div className="login-brand"><div className="brand-mark">⚡</div><div><b>DemandIQ</b><span>Demand viewer</span></div></div><div className="user-actions"><div className="user-identity"><div className="header-avatar">{profile.name.charAt(0)}</div><div><b>{profile.name}</b><span>{profile.email}</span></div></div><div className="user-badge"><Users size={15} /> User access</div><button className="logout-btn" onClick={onLogout}>Sign out</button></div></header><section className="user-content"><ViewHeader eyebrow="DEMAND OVERVIEW" title="Customer demand" description="A focused view of forecasts and market momentum." /><UserProfile profile={profile} /><div className="stats-grid"><StatCard icon={TrendingUp} title="Trending products" value="18" change="+24.8% this week" tone="blue" /><StatCard icon={Sparkles} title="Predicted demand" value="3,370" change="+18.2% this month" tone="purple" /><StatCard icon={Gauge} title="Model confidence" value="91.6%" change="+3.7% this week" tone="green" /></div><div className="grid-two"><section className="panel chart-panel"><div className="panel-head"><div><h2>Demand momentum</h2><p>Predicted demand vs. social mentions</p></div></div><DemandChart data={trendData} /></section><section className="panel"><div className="panel-head"><div><h2>Top demand signals</h2><p>Products customers are asking for</p></div></div><div className="product-list">{products.slice(0, 4).map((product, index) => <div className="product-row" key={product.id || index}><div className="product-icon">{['🎧', '⌚', '📱', '🔊'][index % 4]}</div><div className="product-info"><b>{product.name}</b><span>{product.category || 'General'}</span></div><div className="demand-number"><b>{[1150, 860, 640, 720][index % 4]}</b><span>predicted</span></div></div>)}</div></section></div></section></main>; }

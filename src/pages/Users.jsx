import React from 'react';
import { Users, Trash2 } from 'lucide-react';
import ViewHeader from '../components/common/ViewHeader';
export default function UsersPage({ users, onDelete }) { return <><ViewHeader eyebrow="ACCESS MANAGEMENT" title="Users" description="Manage customer access to demand insights." /><section className="panel"><div className="panel-head"><div><h2>Team members</h2><p>Viewer accounts can access demand information only.</p></div><div className="ai-badge"><Users size={14} /> {users.length} users</div></div><div className="user-list"><div className="managed-user user-column-head"><span>USER</span><span>ROLE</span><span>STATUS</span><span>CREDENTIALS</span><span>ACTION</span></div>{users.map(user => <div className="managed-user" key={user.email}><div className="managed-details"><b>{user.name}</b><span>{user.email}</span></div><span className="managed-role">Demand Viewer</span><div className="managed-activity"><i /><span>Active</span></div><span className="masked-credentials">•••••••• <small>Password protected</small></span><button className="delete-user-btn" onClick={() => onDelete(user.email)} aria-label={`Delete ${user.name}`}><Trash2 size={15} /></button></div>)}</div></section></>; }
